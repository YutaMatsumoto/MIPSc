#include <iostream>
#include <vector>

#include "../Compiler/Symbol.h"
#include "Nodes.h"

#ifndef TREE_H_GUARD
#define TREE_H_GUARD

// ---------------------------------------------------------------------------
// Abstract Symtax Tree

/*
struct RootNode : public Node {
	RootNode() 
		: Node(NULL)
	{}

	Code toIntermediateCode();

	Code toMips();

};

class AST {
public : 
	AST() 
	{}

	std::vector< std::string > toIntermediateCode();

	std::vector< std::string > toMIPS();

private:
	RootNode root;

};
*/

#endif
