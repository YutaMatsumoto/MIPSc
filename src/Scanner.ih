// Generated by Flexc++ V0.98.00 on Tue, 26 Feb 2013 15:00:00 -0800

// $insert class_h
#include "Scanner.h"
#include "Parser.h"

void Scanner::scannerError()
{

	std::cerr << "Invalid Token Line " << lineNr()  << std::endl;
	
	exit(0);

}

void Scanner::updateLocation()
{
	
	parser->getLocationStruct()->first_column = parser->getLocationStruct()->last_column;
	
	parser->getLocationStruct()->last_column = parser->getLocationStruct()->first_column + matched().size();
	
}

void Scanner::newLine()
{
	parser->getLocationStruct()->first_line += 1;
	parser->getLocationStruct()->last_line = parser->getLocationStruct()->first_line;
	
	parser->getLocationStruct()->first_column = 0;
	parser->getLocationStruct()->last_column = 0;
	
	parser->getLocationStruct()->text = (char*) "";
	
}

void Scanner::tab()
{
	
	parser->getLocationStruct()->first_column = parser->getLocationStruct()->last_column;
	
	parser->getLocationStruct()->last_column = parser->getLocationStruct()->first_column + Scanner::tabWidth;
}

void Scanner::dumpSymbolTable()
{

	stab->dumpTable( "symboltable.txt" );

}